variables:
  CPRAN_GIT: https://gitlab.com/cpran/plugin_

before_script:
  - rm -rf ../plugin_{testsimple,utils,strutils,selection}
  - git clone ${CPRAN_GIT}testsimple
  - git clone ${CPRAN_GIT}utils
  - git clone ${CPRAN_GIT}strutils
  - git clone ${CPRAN_GIT}selection
      
# This file is generated by GitLab CI
Praat 5.4.22:
  script:
    - prove -v --exec "praat-5.4.22 --ansi --pref-dir=../.."
  tags:
    - cpran
  allow_failure: true

Praat 6.0.03:
  script:
    - prove -v --exec "praat-6.0.03 --ansi --pref-dir=../.. --run"
  tags:
    - cpran
  allow_failure: true

Praat 6.0.11:
  script:
    - prove -v --exec "praat-6.0.11 --ansi --pref-dir=../.. --run"
  tags:
    - cpran
  allow_failure: true

Praat 6.0.13:
  script:
    - prove -v --exec "praat-6.0.13 --ansi --pref-dir=../.. --run"
  tags:
    - cpran
  allow_failure: true

Praat 6.0.14:
  script:
    - prove -v --exec "praat-6.0.14 --ansi --pref-dir=../.. --run"
  tags:
    - cpran
